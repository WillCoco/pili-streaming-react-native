/**
 * 组货
 */
import * as React from 'react';
import {
  View,
  StyleSheet,
} from 'react-native';
const AnorchMeScreen = () =>  {
  return (
    <View style={styles.style}>
    </View>
  )
};

AnorchMeScreen.defaultProps = {
};

const styles = StyleSheet.create({
  style: {
    flex: 1,
  }
});

export default AnorchMeScreen;