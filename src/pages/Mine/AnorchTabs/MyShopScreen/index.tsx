/**
 * 组货
 */
import * as React from 'react';
import {
  View,
  StyleSheet,
} from 'react-native';
const MyshopHone = () =>  {
  return (
    <View style={styles.style}>
    </View>
  )
};

MyshopHone.defaultProps = {
};

const styles = StyleSheet.create({
  style: {
    flex: 1,
  }
});

export default MyshopHone;