/**
 * 主播账单
 */
import * as React from 'react';
import {
  View,
  StyleSheet,
} from 'react-native';

const AnchroBill = () =>  {
  return (
    <View style={styles.style}>
    </View>
  )
};

AnchroBill.defaultProps = {
};

const styles = StyleSheet.create({
  style: {
    flex: 1,
  }
});

export default AnchroBill;